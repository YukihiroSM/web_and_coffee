import { UserCreateProjectPage } from '../components';

const page = {
  element: UserCreateProjectPage,
  path: 'PROJECT_CREATE',
  protectedRoute: true,
  key: 'create-project',
};

export default page;
