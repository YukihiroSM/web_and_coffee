import { UserProjectsPage } from '../pages/user-projects.page';

const page = {
  element: UserProjectsPage,
  path: 'USER_PROJECTS',
  protectedRoute: false,
  key: 'user-projects',
};

export default page;
